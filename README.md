Bare `http(s)` URLs in page content are auto-linkified client-side in `_includes/head/custom.html`.

Post visibility flags:
- `show_in_blog: false` hides a post from `/blog/` only.
- `published: false` prevents Jekyll from publishing the post at all, so it will not have a public page, feed entry, or sitemap entry.
